<c:FragmentDefinition xmlns="sap.m"
	xmlns:c="sap.ui.core">

	<Dialog id="dialog1" title="{itemModel2>/CompanyName}">
		<content>

			<VBox class="sapUiSmallMargin">

				<Text text="CustomerID  : {itemModel2>/CustomerID}" />
				<Text text="" />
				<Text text="CompanyName : {itemModel2>/CompanyName}" />
				<Text text="" />
				<Text text="ContactName : {itemModel2>/ContactName}" />
				<Text text="" />
				<Text text="ContactTitle: {itemModel2>/ContactTitle}" />
				<Text text="" />
				<Text text="Address     : {itemModel2>/Address}  {itemModel2>/City}" />
				<Text text="" />
				<Text text="PostalCode  : {itemModel2>/PostalCode}" />
				<Text text="" />
				<Text text="Country 	: {itemModel2>/Country}" />
				<Text text="" />
				<Text text="Phone 		: {itemModel2>/Phone}" />
				<Text text="" />
				<Text text="Fax 		: {itemModel2>/Fax}" />

			</VBox>

		</content>

		<beginButton>
			<Button text="Close" press=".onCloseDialog"/>
		</beginButton>
	</Dialog>

</c:FragmentDefinition>